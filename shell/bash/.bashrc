# .bashrc

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# [ -f $HOME/dotfiles/shell/bash_ps1 ] && . $HOME/dotfiles/shell/bash_ps1
[ -f $HOME/dotfiles/shell/bash_aliases ] && . $HOME/dotfiles/shell/bash_aliases
