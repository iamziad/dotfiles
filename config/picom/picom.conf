# Translated from home/services/picom.nix (services.picom).
# Only backend + vsync were ever set on the Nix side — this is the same,
# left deliberately minimal so it's easy to extend (shadows, fading,
# opacity rules, ...) if you want more later.

backend = "glx";
vsync = true;
